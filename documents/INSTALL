                            Installation instruction
                            ~~~~~~~~~~~~~~~~~~~~~~~~
1. FreeBSD
~~~~~~~~~~
a. From binary (txz) package

   Use package manager to install/remove application.

b. From source package

   Build and install:
   # tar -xzf tubes2oe-2.2.tar.gz
   # ./configure
   # make
   # make install

   Remove:
   # make uninstall

2. Linux
~~~~~~~~
a. From binary (deb, package, rpm, txz) package

   Use package manager to install/remove application.

b. From binary (bin) package

   Install:
   # ./tubes2oe-2.2-i586.bin

   Remove:
   # /opt/tubes2oe/uninstall

c. From binary (tar.gz) package

   Install:
   # cp tubes2oe-2.2-i586.tar.gz /opt
   # cd /opt
   # tar -xzf tubes2oe-2.2-i586.tar.gz
   # cd tubes2oe
   # ./create_links
            or
   # tar -xzf tubes2oe-2.2-i586.tar.gz
   # cd tubes2oe
   # ./install

   Remove:
   # /opt/tubes2oe/remove_links
   # rm -r /opt/tubes2oe/
            or
   # /opt/tubes2oe/uninstall

d. From source package

   Build and install:
   $ tar -xzf tubes2oe-2.2.tar.gz
   $ ./configure
   $ make
   # make install

   Remove:
   # make uninstall

3. Windows
~~~~~~~~~~
a. From binary (exe) package

   Run installer file and follow intructions.

b. From binary (msi) package

   > msiexec.exe /i tubes2oe-2.2-win32.msi (or double-click to open
                                                   package) 
c. From binary (zip) package

   > unzip tubes2oe-2.2-win32.zip          (or use unpacking application)

d. From source package

   Build and install:
   > tar -xzf tubes2oe-2.2.tar.gz          (or use unpacking application)
   > build.bat
   > build.bat install

   Remove:
   > build.bat uninstall

